@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
    int count = 1;
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h2 class="card-title">Categories</h2>
                  <a class="btn btn-lg btn-info" asp-action="Add">Add Category</a>
                  <div class="table-responsive pt-3">
                    <table class="table table-dark">
                      <thead>
                        <tr>
                          <th>  
                            #
                          </th>
                          <th>
                            Name
                          </th>
                          <th>
                            Settings
                          </th>
                        </tr>
                      </thead>
                      <tbody>
                          @foreach (var category in Model)
                         {
                             <tr>
                              <td>
                                @count
                              </td>
                              <td>
                                @category.Name
                              </td>
                              <td>
                                <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@category.Id">
                                    <i class="mdi mdi-delete"></i>
                                </a>
                                <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@category.Id">
                                    <i class="mdi mdi-grease-pencil"></i>
                                </a>
                              </td>
                                </tr>
                                count++;
                            }
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
           </div>

